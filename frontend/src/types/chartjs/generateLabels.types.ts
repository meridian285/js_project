export type GenerateLabelsTypes = {
    data:
        {
            labels: any[],
            datasets:
                {
                    backgroundColor:
                        {
                            [x: string]: any,
                        }
                }[]
        };
}